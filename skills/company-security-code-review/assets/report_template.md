# 公司安全规范审计报告

## 1. 审计范围

- 项目 / 模块：
- 审计时间：
- 审计输入：
  - 源码路径
  - 主要语言：Java / Node.js / TypeScript / Python
  - 参考规范：`信息系统安全设计Checklist V1.1.xlsx`、`SS-006 康龙临床信息系统安全设计规范`
- 本次覆盖的代码区域：
- 未覆盖或仅抽样的区域：

## 2. 总体结论

- 门禁结论：`通过` / `有条件通过` / `不通过`
- 结论依据：
  - 是否存在 `critical` / `high` 级源码问题
  - 是否存在尚未关闭的关键 `需人工确认` 项
- 风险摘要：

## 3. 明确不符合项

对每个问题至少给出：

- 标题
- 风险等级
- 文件与行号
- 对应公司条款
- 攻击路径或利用方式
- 当前代码证据
- 修复建议

## 4. 条款矩阵

至少覆盖这些类别：

- 敏感信息
- 输入校验 / 输出编码
- 异常处理 / 日志
- 认证 / 会话 / 访问控制
- 密码 / 加密 / 随机数
- 文件上传下载 / 解析
- 接口安全

推荐表头：

| 类别 | 条款 | 状态 | 证据 | 说明 |
| --- | --- | --- | --- | --- |

状态只使用：

- `符合`
- `不符合`
- `需人工确认`
- `不适用`

## 5. 已观察到的正向控制

列出能从源码直接证明的控制，例如：

- 使用参数化查询或 ORM 参数绑定
- token 由安全随机数生成
- 上传接口有扩展名白名单和大小校验
- 统一异常处理避免向客户端回显堆栈
- 下载接口基于资源 ID 索引，不接受原始路径

## 6. 需人工确认项

明确说明“为什么不能只靠源码下结论”，例如：

- HTTPS / TLS 版本与套件
- 登录失败锁定阈值
- MFA 是否强制开启
- 会话超时配置
- 端口开放收敛
- KMS / 密钥托管
- 文件服务器目录执行权限

## 7. 修复优先级建议

- 立即修复：
  - `critical` / `high`
- 版本前关闭：
  - 会影响门禁结论的 `medium`
- 后续治理：
  - 低风险问题
  - 规范化重构
  - 补充测试 / 安全基线

## 8. 残余风险

- 本次未覆盖：
- 依赖外部系统或部署态的风险：
- 建议追加验证：

## 9. 附录

- 使用的检索命令：
- 使用的预扫描脚本：
- 关键参考资料：
